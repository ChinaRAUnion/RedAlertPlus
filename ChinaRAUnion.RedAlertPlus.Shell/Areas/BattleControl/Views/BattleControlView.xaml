<Page
    x:Class="ChinaRAUnion.RedAlertPlus.Shell.Areas.BattleControl.Views.BattleControlView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ChinaRAUnion.RedAlertPlus.Shell.Areas.BattleControl.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="Black" Loaded="{x:Bind ViewModel.OnLoaded}" PointerMoved="{x:Bind ViewModel.OnPointerMoved}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <SwapChainPanel Loaded="{x:Bind ViewModel.SetupSwapChainPanel}"/>
        <Grid Grid.Column="1" Width="100">
            
        </Grid>
    </Grid>
</Page>
